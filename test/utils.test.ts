/*
import mocha, { describe, it } from 'mocha';
import Kit from '..';
import '../src/polyfills';

describe('Utils test', () => {
  it('hasConditions', () => {
    const obj = {
      a: 'hello',
      b: true,
      c: 11,
      d: undefined,
      e: null,
      f: { a: { b: 1 } }
    };
    console.log(Kit.hasConditions(obj));
    console.log(Kit.hasConditions(obj, { a: 'hello' }));
    console.log(Kit.hasConditions(obj, { b: true }));
    console.log(Kit.hasConditions(obj, { c: 11 }));
    console.log(Kit.hasConditions(obj, { d: undefined }));
    console.log(Kit.hasConditions(obj, { e: null }));
    console.log(Kit.hasConditions(obj, { 'f.a': { b: 1 } }));
    console.log(Kit.hasConditions(obj, Kit.isNotVoid, { c: Kit.isNumber }));
  });
});
*/
